{
  "name": "affiliate-machine",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "pnpm --filter @affiliate/web dev",
    "build": "pnpm --filter @affiliate/web build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r typecheck"
  },
  "dependencies": {
    "next": "16.3.6"
  }
}
