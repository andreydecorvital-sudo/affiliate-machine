services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: affiliate
      POSTGRES_PASSWORD: affiliate
      POSTGRES_DB: affiliate_machine
    ports:
      - "5432:5432"
    volumes:
      - affiliate_pg:/var/lib/postgresql/data
volumes:
  affiliate_pg:
